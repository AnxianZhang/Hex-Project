package player;

public class Human extends Player{
    public Human(){
        super(Identity.HUMAIN);
    }
    // public void play(){
    //     assert numCase < Math.pow(p.taille(), 2) && numCase >= 0;
    //     if (p.isPlayable(numCase)){
    //         p.play(numCase, this.numPlayer);
    //     }
    // }
}
